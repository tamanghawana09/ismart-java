<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Student</title>
</head>
<body>
    <h1>Edit Student</h1>

    <form th:action="@{/students/update/{id}(id=${student.id})}" method="post">
        <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}" />

        <label>First Name:</label>
        <input type="text" name="fname" th:value="${student.fname}" required /><br><br>

        <label>Last Name:</label>
        <input type="text" name="lname" th:value="${student.lname}" required /><br><br>

        <label>Email:</label>
        <input type="email" name="email" th:value="${student.email}" required /><br><br>

        <label>Number:</label>
        <input type="text" name="number" th:value="${student.number}" /><br><br>

        <label>Birthdate:</label>
        <input type="date" name="birthdate" th:value="${student.birthdate}" /><br><br>

        <label>SEE Marks:</label>
        <input type="number" step="0.01" name="marksSEE" th:value="${student.marksSEE}" /><br><br>

        <label>Plus2 Marks:</label>
        <input type="number" step="0.01" name="marksPlus2" th:value="${student.marksPlus2}" /><br><br>

        <label>Course:</label>
        <input type="text" name="course" th:value="${student.course}" /><br><br>

        <label>Gender:</label>
        <input type="text" name="gender" th:value="${student.gender}" /><br><br>

        <button type="submit">Update Student</button>
    </form>

    <br>
    <a href="/students">Back to List</a>
</body>
</html>
